\section{The parsing layer}
\subsection{Spreadsheet grammar}
( Here I will write about the Spreadsheet grammar ands its detail and also list the detailed grammar used in the parser)
\subsection{Transformers}
( Here I will write about how the system transforms the AST when we have edited something. The transformers are cell reference transformer, literal transformer, expression replacement transformer and also how the system swaps or adds function arguments)
\subsection{Serializer}
( Here I will write about how the system serializes the AST back into a formula. Maybe I will also make a diagram to show the flow of these)